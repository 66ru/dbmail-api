require "imap4flags";

#rule=rule4
#require={"imap4flags":true}
if not header :contains ["Cc", "To"] "from" {
    setflag "\\Seen";
}

#rule=rule5
if allof(size :over 154,
         size :under 156) {
    discard;
}

